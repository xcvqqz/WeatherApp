CREATE TABLE Sessions (
   session_id UUID PRIMARY KEY NOT NULL,
   user_id INT NOT NULL,
   expires_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
   FOREIGN KEY (session_id) REFERENCES users(user_id) ON DELETE CASCADE
);